<div class="row">
  <div class="col-lg-12">
    <h1>ユーザー情報一覧</h1>
  </div>
  <div class="col-lg-6">
    <p>ユーザー情報が表示されます。</p>
  </div>
  <table class="table table-striped">
    <thead>
      <th>ユーザー名</th>
      <th>リンク</th>
    </thead>
    <tbody>
      <% @users.each do |user| %>
        <tr>
          <td><%= user.name %></td>
          <td><%= link_to '詳細', user, class: "btn btn-default btn-xs" %></td>
        </tr>
      <% end %>
    </tbody>
  </table>
</div>
